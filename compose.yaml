name: mjm

services:
  development:
    container_name: development
    build:
      context: .
      target: development
    ports:
      - '5173:5173'
    develop:
      watch:
        - path: ./
          target: /usr/local/app
          action: sync
        - path: package.json
          action: rebuild
